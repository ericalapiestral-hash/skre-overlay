# CLAUDE.md — SKRE 오버레이

## 이 프로젝트가 뭔가

세븐나이츠 리버스 **파괴신·공성전** 전용 PC 오버레이. 게임 위에 항상 떠 있으면서
스킬 순서를 보여주고, 화면의 턴 숫자를 읽어 자동으로 다음 단계로 넘긴다.

길드봇(`guild-bot`)의 `overlay/` 폴더에 있던 것을 **처음부터 다시 만든** 저장소다.
옛 오버레이는 지금도 그 저장소에 남아 있지만, 여기가 후속작이다.

패키지명 `skre-overlay`. Electron 33 + 순수 CommonJS. 빌드 단계 없음.

## 왜 다시 만들었나 (그리고 뭘 고쳤나)

쓰는 사람이 꼽은 불만 셋이 그대로 재작성의 목표였다.

### 1. 턴 자동 인식이 부정확하다

**턴은 숫자가 아니라 `16 / 70`이다** (지금 턴 / 최대 턴). 실제 게임 스크린샷을 받고서야
알았다. 슬래시를 모르면 "1670"이나 (자릿수 제한에 걸려) "6/7" 같은 조각으로 읽힌다.
그래서 인식기가 **슬래시를 찾아 왼쪽까지만** 읽는다. 자세한 것은 아래 "슬래시" 항목.

**진짜 원인은 인식 알고리즘이 아니라 캡처였다.** 옛 코드는 화면을 잡을 때
크기 제약(`maxWidth`/`maxHeight`)을 주지 않았다. 크로미움은 그러면 **1280×720으로
줄여서** 캡처한다 — 1440p·4K 화면에서는 턴 숫자가 절반 이하로 뭉개진 채
인식기에 들어간다. 어떤 인식기를 붙여도 한계가 있었다.

지금은 `capture:source`가 그 모니터의 **실제 픽셀 크기**를 같이 돌려주고, 렌더러가
그 값을 `minWidth`/`maxWidth`에 못 박는다. **이 코드를 지우지 말 것.**

인식기 자체도 다시 썼다.

- **명암 두 방향을 모두 시도한다.** 예전엔 "화면에서 적은 쪽이 글자"라고 단정해서,
  영역에 밝은 패널이 조금만 걸쳐도 통째로 뒤집혔다.
- **자릿수를 더 많이 찾은 쪽이 이긴다.** 점수만 보고 고르면 "60"이 한 덩어리로 잡힌
  쪽이 경쟁자가 없어 마진이 커서 이겼고, 그래서 7 하나로 읽혔다.
- **붙어 버린 숫자를 나눈다.** 확대하면 "12"의 획이 이어져 한 덩어리가 되고, 그게
  8로 눌렸다. 나눌지 말지는 어림짐작이 아니라 **인식 점수로 비교해서** 정한다.
- **구멍의 위치까지 본다.** 개수만 보면 6(아래)·9(위)·0(가운데)를 못 가른다.
- **빌드에 나오는 턴을 후보로 쓴다.** 후보 안에서 1·2등을 다시 가르고, 후보에 없는
  숫자를 확신하면 문턱을 3배로 올려 한 번 더 의심한다.
- **여러 프레임을 모아 판단한다**(`vote.js`). 한 프레임 튀었다고 단계가 넘어가지 않는다.
- **사용자가 자기 게임 폰트를 가르칠 수 있다**([가르치기]). 기본 대조표보다 정확하다.
- **크롭을 64px 근처로 키워서 읽는다**(`CROP_TARGET_HEIGHT`). 이게 정확도를 제일 크게
  바꾼다 — 아래 "속도" 항목 참고.

#### 슬래시 (`16 / 70`)

슬래시를 가리는 값 셋(`SLASH`)은 `node tools/tune-slash.js`가 **앱과 같은 조건에서**
재서 골랐다. 앱 조건에서는 `diag`(위쪽 무게중심 x − 아래쪽 무게중심 x)가 완전히 가른다:
숫자 −0.15~**0.23**, 슬래시 **0.38**~0.61. 문턱은 그 틈의 가운데다. `ratio`·`fill`은
갈라 주지는 않지만 처음 보는 폰트에서 엉뚱한 덩어리가 걸리는 걸 막는 울타리로 같이 둔다.

- **원본 크기로 재지 말 것.** 처음에 낱글자를 원본 크기로 재서 문턱을 골랐더니 오독이
  **일곱 배**로 늘었다. 확대하면 획이 번져 ratio도 fill도 달라진다. bench가 예전에
  저지른 것과 똑같은 실수다.
- **거짓 양성(숫자를 슬래시로 봄)이 0**인 것을 먼저 만족시키고, 그 안에서 **양쪽 여유의
  작은 쪽**을 키워 골랐다. 한쪽만 넉넉하면 반대쪽으로 뚫린다.
- 슬래시는 **두 번 찾는다**: 덩어리 단계에서 한 번, **나눈 뒤에 한 번 더.** 작은 글자에서는
  슬래시가 옆 숫자에 붙어("`/7`") 덩어리 단계에서는 안 보이고 나눠 놓고서야 보인다.
  나눈 뒤 자르기를 빼면 그 명암이 통째로 버려지고, **뒤집힌 명암에서 잡힌 글자 구멍**이
  유일한 후보가 되어 빌드 턴에 맞춰 자신 있는 오답을 낸다 (실제로 "0/70"이 "12"로 읽혔다).
- 명암을 고르는 "자릿수를 더 많이 찾은 쪽이 이긴다"는 **자르기 전** 글자 수로 센다.
  자른 뒤로 세면 옳은 쪽이 한 자리로 줄어드는 바람에 구멍 두 개한테 진다 ("0/70"→"33").
- 지금 수치: "N / M" 표본 1008장에 대해 **맞음 96.8% · 오독 0% · 모르겠음 3.2%**.
  못 읽는 3.2%는 슬래시가 옆 숫자에 완전히 붙어 버린 경우인데, 억지로 읽는 것보다 낫다.

tesseract는 **없앴다.** 76MB와 첫 실행 1분 다운로드, asar·워커 문제를 다 데려오는데
0~9 열 개를 가리는 데는 필요 없다. 지금 이 앱은 **런타임 의존성이 0개다.**

### 2. 빌드가 안 보이거나 틀리게 파싱된다

옛 파서는 `## 스킬 순서` + `### N라운드` + 백틱 `` `0턴` `` 셋이 다 맞아야만 읽었고,
**단계가 0개인 빌드는 목록에서 통째로 뺐다.** 도감엔 있는데 오버레이엔 없었다.

지금은 읽는 방법을 여러 겹으로 두고, 실패해도 숨기지 않는다.

- 백틱 없이도, 굵은 글씨 제목도, 표(테이블)도, 목록도 읽는다
- 라운드 표기는 `2라운드`·`라운드 2`·`2R`·`R2`·`2페이즈` 모두 같은 것으로 본다
- `스킬 순서` 제목이 아예 없으면 본문 전체에서 턴 표시를 찾는다
- 액션의 슬래시를 안 잘라먹는다 (예전엔 "미호/나타 위"가 "미호"로 잘렸다)
- 그래도 못 읽으면 `strategy: 'none'`으로 표시하고 **도감 본문을 그대로 보여준다**

### 3. 화면·조작이 불편하다

- 지금 할 행동 한 줄이 확실히 크다. 나머지는 흐리게.
- 단계를 **전부** 보여주고 현재 단계로 자동 스크롤한다 (남은 게 뭔지 보여야 한다)
- 손으로 단계를 옮겨도 자동이 안 꺼진다 — 옮긴 자리에서 계속 따라간다
- 글자 크기·배경 진하기·인식 주기를 슬라이더로 조절
- 공성전 탭은 오늘 요일 보스를 먼저 골라 준다
- **턴 영역이 처음부터 맞춰져 있다** (`src/shared/regions.js`). 파괴신·공성전은 턴이 늘
  화면 왼쪽 위 같은 자리라 매번 드래그할 이유가 없다. 값은 **화면 대비 비율**이라
  해상도가 바뀌어도 같은 자리를 가리킨다. 다만 이 값은 스크린샷을 **눈으로 재서** 넣은
  것이라 실제 화면에서 어긋날 수 있다 — 그때는 [턴 영역]으로 잡고, 그 값으로 여기를 고친다.
  앱이 알아서 쓴 기본값은 **설정에 저장하지 않는다.** 저장해 버리면 나중에 기본값을
  고쳐도 이미 저장된 옛 값에 발이 묶인다.
- 설정·가르치기는 접어 두고, 한 번에 하나만 열린다

## 그다음 — 기능을 고성능으로 (2차 작업)

디자인은 나중에 통째로 바꿀 예정이라 손대지 않고, 기능 성능만 끌어올렸다.

### 프레임당 비용 (게임과 CPU를 나눠 쓴다)

한 장 읽는 데 **14.3ms → 0.66ms**. 같은 결과를 내면서 그렇게 됐다.

- **격자를 비트로 다룬다.** 336칸을 32비트 낱말 11개에 담고, 겹치는 칸 세기를
  비트 AND + popcount로 한다.
- **부풀린 모양을 미리 만들어 둔다.** 예전엔 대조 한 번마다 양쪽을 새로 부풀렸다 —
  템플릿이 130개니 같은 계산을 프레임마다 260번 되풀이했다. **여기가 전체의 90%였다.**
- **상한으로 가지친다.** 획 수 차이만 봐도 넘을 수 없는 점수가 정해지므로, 그 숫자의
  현재 1등을 못 넘길 템플릿은 계산을 건너뛴다. 결과는 한 자리도 안 바뀐다
  (테스트가 가지치기 없는 계산과 대조한다).
- **덩어리를 라벨 지도로 다룬다.** 픽셀 목록을 안 만들어 프레임마다 생기는 쓰레기가 없다.
- **크롭 확대 목표를 96px → 64px로 낮췄다.** 크게 키울수록 좋을 줄 알았는데 아니었다.
  96px에서는 부드럽게 늘어나는 과정에서 옆 숫자와 획이 붙어 자릿수를 잃는다.
  64px가 **더 정확하고 두 배 빠르다.**
- **이진화 문턱값을 여러 번 시도하려다 재 보고 뺐다.** 원본 크기에서는 도움이 됐지만
  64px로 키운 뒤에는 오히려 오답을 만들고 3배 느렸다. 구조는 남겨 뒀다
  (`readTurn(..., { thresholdOffsets: [0, -8, 8] })`).

그래서 **인식 주기를 600ms → 250ms로 낮췄다.** 이게 "턴이 바뀌고 화면이 따라오기까지"의
시간이다. 600ms는 인식이 14ms 걸리던 때 잡은 값이었다. 캡처 장수도 주기에 맞춰 받으므로
(초당 10장 → 5장) 화면을 통째로 받아 오는 부담은 오히려 줄었다.

### 그 밖의 프레임·갱신 비용

- **도감은 메인이 들고, 화면에는 가벼운 목록만 보낸다.** 예전엔 갱신마다 빌드마다의
  본문과 펼친 단계 자료를 통째로 넘겼다 — 빌드 120개·본문 3KB면 **405KB**가 오갔다.
  지금은 목록에 필요한 것만 보내 **19KB**다. 단계와 본문은 id로 물어본다.
- **단계 줄은 흐름이 바뀔 때만 다시 만든다.** 자동 인식은 주기마다 도는데 그때마다
  줄을 새로 만들면 화면이 깜빡이고 스크롤이 튄다. 위치만 바뀌면 표시만 옮긴다.
- **새 화면이 왔을 때만 센다.** 시간만 보고 돌리면 같은 화면을 두 번 읽고, 투표가
  그걸 "두 프레임이 같게 읽혔다"로 세어 오독을 확정할 수 있다.
  다만 화면이 한동안 안 오면 조용히 멈추므로 안전줄(watchdog)을 같이 둔다.

## 그다음 — 연출·밀림에도 끝까지 따라가기 (3차 작업)

쓰는 사람이 실제 전투에서 겪는 세 가지를 그대로 가져왔다:
**스킬 연출이 턴 숫자를 가린다**, **반쯤 가려지면 엉뚱하게 읽힌다**, **게임 효과로 턴이
한두 턴 뒤로 밀린다** (오독이 아니라 실제). 옛 구조(투표 + 단계 이동 규칙)는 이 셋을
따로따로 땜질하고 있었고, 특히 뒤로 가는 숫자를 "라운드 안에서는 되돌아갈 수 있다"고
처리해서 밀림 한 번에 이미 한 단계를 다시 보여줬다.

### 추적기 (`src/shared/follower.js`)

투표와 단계 이동을 하나의 상태 기계로 합쳤다. 원칙은 **앞으로는 가볍게, 뒤로는 무겁게**:
턴은 원래 오르기만 하므로 앞으로 가는 읽기는 대개 진짜고, 뒤로 가는 읽기는
밀림·오독·재시작·리셋 라운드 전환 넷 중 하나인데 그중 셋의 답이 "그대로 둔다"다.

정책은 `test/scenarios/README.md`에 **P1~P9**로 적혀 있다. **규칙을 고치면 거기부터 고칠 것.**
핵심만:

- 작은 뒤로(1~3턴)는 밀림 — 단계 그대로. 큰 뒤로도 그대로 — 재시작·전환만 예외.
- 재시작은 첫 턴 근처 숫자가 **600ms 가려짐 없이** 이어져야 믿는다. 진짜 재시작은 0이
  몇 초씩 가만히 있으니 기다려도 손해가 없고, 연출 중 오독("16"→"1")은 깜빡인다.
- 두 단계 이상 건너뛰는 이동은 **3프레임** 이어져야 한다. 그래도 잘못 뛰었으면 3초 안에
  뛰기 전 위치에 맞는 숫자가 돌아올 때 **되돌린다**. 재시작도 같은 방식으로 되돌린다.
- "이어진다"는 같은 값이 아니라 **같은 조건**이 이어지는 것이다. 전투 중엔 턴이
  프레임마다 바뀌므로(9, 10, 11) 같은 값을 기다리면 영영 못 넘어간다 — 처음에 그렇게
  만들었다가 시나리오에서 바로 걸렸다.
- **뒤로 가는 읽기에는 P5·P6을 P3(밀림)보다 먼저 본다.** 턴 3에서 0으로 재시작하는 건
  가장 흔한 경우인데 3→0은 밀림 조건에도 들어맞는다. 밀림을 먼저 보면 기준이 0으로
  내려앉고 그 뒤의 0들이 "앞으로"가 되어 재시작 이어짐을 아예 못 센다.
- **되돌리기(P8)는 "뛴 곳보다 뛰기 전에 더 가까운 값"일 때만 한다.** 그 조건이 없으면
  0으로 재시작한 직후의 0들이 되돌리기 범위에 걸려 **재시작이 스스로를 되돌린다.**
- **첫 읽기(앱 시작·손으로 옮긴 직후)도 P7을 거친다.** 안 그러면 오독 한 프레임에 끝으로
  순간이동하고, 그 오독이 기준이 되어 그 뒤 진짜 턴이 전부 "큰 뒤로"가 되며 갇힌다.
  그래서 P7이 기다리는 동안에는 **기준(believed)을 잡지 않는다.**
- 리셋 빌드는 단계 목록에서 스스로 알아낸다 (다음 라운드 첫 턴 ≤ 이 라운드 마지막 턴).
  리셋 경계 너머로는 앞으로 찾지 않는다 — 그 너머는 다른 눈금이다. 옛 코드는 여기서
  라운드 끝을 지나면 빌드 마지막 단계로 튀었다.

### 시나리오로 확인한다 (`test/scenarios/*.json`)

실제 전투를 100ms 프레임 단위로 적은 파일들이다. 가려짐(null)·오독·밀림·재시작·리셋
전환을 프레임 열로 적고, 정책에서 도출되는 기대(어느 프레임까지 어느 단계 / 이 구간
내내 그대로)를 붙인다. `test/scenario.test.js`가 전부 돌리고, 틀리면 궤적을 통째로 보여준다.

- 여러 관점(연출·밀림·재시작·리셋빌드·빠른진행·오독)에서 뽑은 것과, 추적기를 **깨뜨리려고**
  쓴 것(`adv-*`)이 섞여 있다. 새 규칙을 넣으면 시나리오부터 쓸 것.
- 시나리오가 실제로 잡아낸 것들 — 규칙 하나가 다른 규칙을 삼키는 게 대부분이었다:
  - **밀림이 정당한 뛰기를 되돌렸다.** 촘촘한 빌드(4·6·8턴)에서 두 단계 뛴 직후의 2턴 밀림이
    "뛰기 전 턴 근처"라 P8 되돌리기 조건을 그대로 만족했다. 뛴 곳보다 3턴 안쪽으로만 낮은
    값은 되돌릴 증거가 아니라 밀림이다.
  - **라운드를 미리 골라 두면 오히려 한 라운드를 건너뛰었다.** 리셋 빌드에서 2라운드로 손수
    옮기면 화면엔 아직 1라운드 눈금이 남아 있어서, 2라운드가 0턴으로 시작하는 순간 그 0이
    P5에 걸려 3라운드로 튀었다 (P9b).
  - **받아들이지 않은 흐린 읽기가 이어짐을 안 끊었다.** 또렷한 오독 2프레임 사이에 흐린
    1프레임이 끼면 P7의 3프레임이 채워져 순간이동했다. 흐린 프레임이 낀 것 자체가
    깜빡인다는 증거다.
  - **손으로 옮긴 뒤 뒤로 가는 규칙이 한 번도 안 불렸다.** 화면이 옮긴 자리보다 뒤이면
    그 값을 기준으로 못 박았는데, 그러면 그 뒤 읽기가 전부 "앞으로"가 된다.
- 위에 적은 네 가지 실수는 **전부 시나리오가 잡았다.** 눈으로 읽어서는 안 보이는
  것들이라(특히 "재시작이 스스로를 되돌린다") 이 방식을 버리지 말 것.
- expect를 적을 때 제일 자주 틀리는 것: **지금 단계의 턴을 지나면 곧바로 다음 단계가
  "지금"이다.** 4턴 단계를 보다 턴이 5가 되면 8턴 단계가 지금 할 행동이다.
- 프레임 간격은 100ms로 가정한다. 인식 주기 기본값(`tickMs`)도 100ms다 — 추적기의
  "몇 프레임 이어져야"는 ms로도 같이 재므로 주기를 바꿔도 뜻은 유지된다.

### 화면 쪽

- 인식 주기 250ms → **100ms**. 한 장 읽는 데 1ms 안팎이라 가능하다.
- **프레임을 흘려보내는 문턱은 주기가 아니라 캡처 간격을 기준으로 잡는다** (반 장 분량).
  주기를 문턱으로 쓰면 **읽는 속도가 반으로 깎인다** — 캡처가 주기에 맞춰 오는데
  도착이 조금이라도 이르면 그 장을 흘려보내고 두 배 뒤의 다음 장을 기다리기 때문이다.
  실제로 100ms 주기에서 초당 5.5회밖에 안 읽고 있었다. 지금은 캡처 장수를 주기와 같게
  요청하고 온 장은 다 읽어서, 50ms~1000ms 어느 주기에서도 목표 속도가 그대로 나온다.
  설정 패널에 **실제 인식 속도**(초당 몇 회)를 띄워 눈으로 확인할 수 있게 했다.
- 상태줄에 추적기가 **왜 안 움직이는지**를 보여준다 ("재시작인지 확인 중", "턴이 뒤로 밀림 — 단계 유지").
  "왜 안 넘어가지?"를 없애려는 것이다.

## 명령어

```bash
npm install
npm start          # 실행
npm test           # 단위 + 전투 시나리오 + 부팅 스모크 (185개)
npm run typecheck  # JSDoc 기반 타입 검사 (빌드 단계 없음)
npm run check      # typecheck + test
npm run doctor     # 도감을 어디서 찾았고 몇 개 읽었는지 (화면 없이)
npm run bench      # 숫자 인식기 정확도
node tools/tune-slash.js           # 슬래시 문턱 다시 재기
node tools/replay.js <기록.json>   # 실제 전투 기록 되돌려 보기
npm run templates  # 대조표 다시 뽑기 (그 컴퓨터의 폰트가 더해진다)
npm run fixtures   # 성능 표본 다시 만들기
npm run dist       # 포터블 exe
```

## 구조

```
src/
  main/
    index.js    앱 시작 · 창 · IPC · 전역 단축키 · --doctor
    catalog.js  builds.json 찾기 · 읽기 · 감시
    engine.js   픽셀 한 장 → 지금 몇 번째 단계인지 (읽기 → 따라가기)
    config.js   설정 저장 (임시파일 → rename)
    recorder.js 전투 기록 — 실제 게임에서 벌어진 일을 시나리오로 뽑아낸다
  shared/       화면도 캡처도 모르는 순수 로직
    turnReader.js 숫자 인식기
    follower.js   턴 추적기 — 연출·밀림·재시작을 견디며 단계를 따라간다
    steps.js      도감 본문 → 단계
    tracker.js    라운드 나누기 도우미
    regions.js    턴 표시가 화면 어디에 있는지 (파괴신 기본 위치)
    templates.json
  preload/      contextBridge 통로
  renderer/     화면 (overlay · picker)
tools/          make-templates · make-fixtures · bench-reader · tune-reader · tune-slash · replay
test/           단위 테스트 + fixtures/digits.json.gz + scenarios/*.json (전투 시나리오)
```

### 실제 게임에서 벌어진 일을 여기로 가져온다 (`src/main/recorder.js`)

**시나리오 82개는 전부 상상해서 적은 것이다.** 진짜 전투에서 어떤 숫자가 실제로
읽혔는지는 이 저장소 안에서 알 방법이 없다 — 여기엔 게임도, 게임 폰트도, 윈도우도 없다.
그래서 "여기서 안 넘어갔어"라는 얘기를 들어도 재현할 수가 없었다. 그 구멍을 메우는 길이다.

자동 인식이 도는 동안 프레임을 고리 버퍼에 늘 담아 두고(10분치), 설정의
**[전투 기록 저장]** 을 누르면 바탕화면에 JSON 한 장이 나온다. 그 파일로 두 가지를 한다.

```bash
node tools/replay.js ~/바탕화면/skre-기록-20260904-1530.json
node tools/replay.js 기록.json --from 120 --to 180        # 구간만
node tools/replay.js 기록.json --scenario "이름" > test/scenarios/real-01.json
```

- **추적기 재생** — 프레임마다 단계가 어떻게 움직였는지 궤적을 보여준다. 단계가 바뀐 줄에
  `◆`, 그때의 화면과 다르면 `(그때는 N)`. "왜 안 넘어갔지"를 프레임 번호로 찾는 도구다.
- **인식기 다시 재기** — 기록에 담긴 크롭 표본을 지금 인식기에 다시 넣는다. `npm run bench`의
  표본 792장은 **우리가 고른 폰트**로 그린 것이라, 게임 폰트에서 어떤지는 이 표본으로만 안다.

시작 버튼을 따로 두지 않은 이유: 이상한 걸 본 사람은 **그 일이 벌어진 뒤에** 누른다.
표본은 종류별로 상한을 둔다(못 읽음 40 · 흐림 40 · 값당 4) — 잘 읽힌 프레임만 잔뜩 모으면
정작 알고 싶은 "왜 못 읽었나"가 한 장도 안 남기 때문이다. 빌드를 바꾸면 기록을 새로
시작한다(다른 빌드의 프레임이 섞이면 되돌려 볼 수 없다).

기록 파일은 **시나리오와 같은 형식**이라 `test/scenarios/`에 그대로 넣을 수 있다. 다만
실제 캡처는 100ms에 딱 맞춰 오지 않으므로 프레임마다 `t`(ms)를 들고 오고, 시나리오
러너가 그걸 본다. `test/recorder.test.js`가 한 바퀴를 통째로 잠근다 — 엔진에 넣고 →
기록하고 → 재생해서 **처음 엔진이 낸 결론과 같은지**. 여기가 어긋나면 기록을 받아도 쓸모없다.

### 앱이 진짜로 뜨는지 (`test/smoke.test.js`)

나머지 테스트는 전부 순수 로직만 본다. 그래서 **전부 초록인 채로 앱이 아예 안 뜨는
일**이 생긴다 — 프리로드 경로 오타, IPC 채널 이름 불일치, contextBridge가 못 넘기는 값,
렌더러 문법 오류, Electron 판올림으로 사라진 옵션. 이 층은 이 테스트만 잡는다.

진짜 Electron을 띄워서(화면 없으면 `xvfb-run`) 창·프리로드 다리·화면 요소를 보고,
**렌더러 안에서 실제 IPC를 왕복시킨다**(도감·설정·엔진). 채널 이름을 한 글자 고치면
바로 빨개진다 — 확인해 두었다. 시험용 도감과 userData를 임시 폴더에 만들어 쓰므로
그 컴퓨터에 뭐가 깔려 있든 결과가 같다.

- 화면도 xvfb도 없으면 **건너뛴다.** 검사를 못 했으면 못 했다고 하지 통과라고 하지 않는다.
- `node:test`는 `skip`이 **빈 문자열이어도 건너뛴다.** `{ skip: why }`로 두면 조건이 다
  맞는 곳에서도 조용히 안 돈다 — 실제로 그래서 한 번 속았다. `why || false`로 둘 것.
- 게임 화면·게임 폰트·윈도우 고DPI는 여기서 확인 못 한다. 그건 실제 PC에서 봐야 한다.

## 알아둘 것

- **판단은 전부 메인에 있다.** 화면은 잘라 넘기고 그리기만 한다. 인식·추적·단계 이동을
  렌더러로 되돌리지 말 것 — 그러면 게임을 직접 돌려보는 것 말고는 확인할 방법이 없어진다.
  지금은 `engine.feed(gray, w, h, now)` 하나로 전 과정이 Node 테스트에 걸린다.
- **추적 규칙은 시나리오로 정한다.** `follower.js`를 고치기 전에 `test/scenarios/`에 그 상황을
  먼저 적고, 정책(README P1~P9)에서 기대를 도출할 것. 정책이 안 정한 상황이면 정책부터 고친다.
- **캡처 크기를 못 박는 코드를 지우지 말 것** (위 1번). 지우면 조용히 1280×720으로 줄어든다.
- **`CROP_TARGET_HEIGHT`를 화면에서 따로 정하지 말 것.** 인식기에 한 값만 두고 preload로
  건네준다. 두 곳에 두면 재는 것과 실제가 조용히 어긋난다 — 실제로 그래서 한동안
  96px로 읽으면서 원본 크기 기준 수치를 믿고 있었다.
- **문턱값은 재서 골랐다.** `npm run bench`가 진짜 폰트로 그린 표본 792장에 대해
  맞음·모르겠음·**틀림**과 걸린 시간을 잰다. 표본을 그린 폰트의 대조표는 빼고 맞추므로
  "처음 보는 게임 폰트" 조건이다. 지금 수치: **792장 전부 맞음 · 오독 0 · 0.66ms**.
  - **벤치는 앱이 실제로 넣는 것과 같은 조건으로 잰다** — 크롭을 `CROP_TARGET_HEIGHT`로
    키운 뒤 빌드 턴을 후보로 넘긴다. 예전엔 원본 크기로 재서, 앱이 겪는 것과 다른 것을
    재고 있었다. 그 상태로 "여러 문턱값이 낫다"는 잘못된 결론을 냈었다.
  - 표본이 다 맞는 상태라 변별력은 참고 줄(후보 없이 / 확대 없이)이 맡는다.
    거기가 나빠지면 무언가 부러진 것이다 — 테스트도 그쪽에 자물쇠를 건다.
  - **틀림이 제일 중요하다.** 틀린 턴을 믿고 단계를 건너뛰면 순서가 통째로 어긋나서,
    아예 못 읽고 가만히 있느니만 못하다. 계수를 만질 일이 생기면 맞음이 아니라
    틀림부터 보고, `tools/tune-reader.js`로 다시 재서 정할 것.
  - 이 수치는 테스트(`test/turnReader.test.js`)에 자물쇠로 걸려 있다.
- **표본은 진짜 폰트로 그린다.** 예전에 대조표(14×24)를 확대해 시험 그림을 만들었더니,
  6의 뚫린 곳이 확대 과정에서 막혀 8처럼 보이는 등 **실제 게임에는 없는 손상**이 생겨
  인식기가 억울하게 틀린 것으로 나왔다. 표본 만드는 방식을 되돌리지 말 것.
- **스킬 순서를 못 읽은 빌드를 숨기지 말 것.** 그게 "빌드가 안 보인다"의 원인이었다.
- **포장하면 `__dirname`이 app.asar 안이 된다.** 도감 경로를 상대경로로 되돌리지 말 것
  (`catalog.js`의 `candidatePaths`가 exe 자리를 따로 잡는다).
- **exe를 공개된 곳에 올리지 말 것.** 도감은 길드 내부 자료다. `builds.json`과 `data/`는
  `.gitignore`에 있다. 저장소가 공개라 **GitHub 릴리스도 공개된 곳이다** — 워크플로
  (`.github/workflows/build.yml`)가 exe를 짓기만 하고 artifact로 남기는 이유가 이것이다.
- **`version`에 `-test.1` 같은 꼬리표를 붙이지 말 것.** NSIS는 버전을 순수 숫자로만
  받아서, `0.9.0-test.1`을 넣으면 포터블 exe를 조립하는 마지막 단계에서 **에러도 없이
  멈춘다** (`1.0.0`은 3분에 끝나는 빌드가 15분을 넘겼다). 시험판 표시는 버전이 아니라
  `portable.artifactName`(파일 이름)에 넣는다.
- **윈도우 exe는 윈도우에서 짓는 게 맞다.** 리눅스에서도 지을 수는 있지만 exe의
  아이콘·이름 같은 메타데이터를 넣는 단계에 wine이 필요하다. 급할 때는
  `-c.win.signAndEditExecutable=false`로 그 단계를 건너뛸 수 있는데, 그러면 exe 속성이
  "Electron"으로 남는다. **이 옵션을 package.json에 박아 넣지 말 것** — 윈도우에서는
  wine이 필요 없어서 제대로 들어간다.
- 접기는 CSS로만 숨기면 안 된다 — 투명한 창이 그대로 게임 클릭을 막는다. 창 자체를 줄인다.
- 클릭 통과(`Ctrl+Alt+L`)를 켤 때는 해제 단축키가 등록됐는지 먼저 본다. 안 그러면 영영 못 끈다.

## 코드 스타일

- 한국어 커밋 메시지, 한국어 UI 문구, 한국어 주석
- 주석은 "무엇을"이 아니라 **"왜 이렇게 했는지"**, 특히 되돌리면 안 되는 이유를 적는다
- 파일명은 영어, 도메인 용어는 한국어(파괴신·공성전·라운드·턴)
- 타입은 JSDoc으로 단다 (`npm run typecheck`). 빌드 단계는 두지 않는다 — `npm start` 한 줄로 뜨는 게 낫다
- 런타임 의존성 0개를 지킨다. 뭔가 추가하려면 정말 필요한지 먼저 의심할 것
