# SKRE 오버레이 0.9.0-test.1 — 시험판 안내

실제 게임에서 한 번도 안 돌려 본 판입니다. **잘 되는지보다 어디서 어긋나는지**를
알고 싶은 것이니, 이상한 게 보이면 아래 [전투 기록 저장]을 눌러 주세요.

## 준비

1. `skre-overlay-0.9.0-test.1.exe` 를 아무 폴더에나 둡니다. 설치는 없습니다.
2. **도감이 필요합니다.** 길드봇이 만들어 두는 `data/builds.json` 을 앱이 알아서 찾습니다.
   - `바탕화면/길드봇/data/builds.json`
   - `바탕화면/guild-bot/data/builds.json`
   - exe 옆의 `data/builds.json` 또는 `builds.json`
   못 찾으면 **[⚙] → [도감 파일 선택]** 으로 직접 고르면 됩니다 (경로를 기억합니다).
3. 게임을 **테두리 없는 창모드**로 켭니다. 전용 전체화면에서는 오버레이가 안 보입니다.

> ⚠ 윈도우가 "알 수 없는 게시자"라고 막을 수 있습니다 (코드 서명이 없어서 그렇습니다).
> `추가 정보 → 실행`으로 넘어가면 됩니다.

## 봐 주셨으면 하는 것

### 1. 턴을 읽나

턴 영역은 **이미 파괴신 자리로 맞춰져 있습니다** (왼쪽 위 `턴 16 / 70`).
빌드를 고르고 **[자동]** 을 켜면 상태줄에 `인식 중 — N턴` 이 떠야 합니다.

- 안 뜨면 **[턴 영역]** 으로 그 자리를 직접 드래그해 보세요. `/ 70` 이 같이 들어가도
  됩니다 — 슬래시를 알아보고 왼쪽 숫자만 읽습니다. 위의 `턴` 글자는 빼 주세요.
- **읽는 숫자가 화면과 같은지** 봐 주세요. 틀린 숫자를 읽는 게 제일 나쁩니다.

### 2. 연출·밀림에서 어떻게 되나

- 스킬 연출로 턴이 가려지는 동안 단계가 튀지 않는지
- 게임 효과로 턴이 한두 턴 뒤로 밀렸을 때 이미 지난 단계로 되돌아가지 않는지
- 라운드가 넘어갈 때 제대로 따라가는지

상태줄에 **왜 안 움직이는지**가 나옵니다 ("재시작인지 확인 중", "턴이 뒤로 밀림 — 단계 유지").

### 3. 속도

**[⚙]** 를 열면 `인식 주기` 옆에 **실제 인식 속도**(초당 몇 회)가 나옵니다.
기본 100ms면 초당 10회 근처가 나와야 합니다. 게임이 버벅이면 주기를 늘려 보세요.

## 이상하면 — [전투 기록 저장]

**[⚙] → [전투 기록 저장]** 을 누르면 방금까지의 전투가 바탕화면에 JSON 한 장으로 나옵니다.

- **미리 켤 필요 없습니다.** 자동이 켜져 있는 동안 계속 담고 있어서, 이상한 걸 본
  **뒤에** 누르면 됩니다 (최근 10분치).
- 그 파일을 주시면 그때 화면에 무슨 숫자가 읽혔는지, 왜 단계가 안 넘어갔는지를
  프레임 단위로 되돌려 보고 그대로 테스트로 만들 수 있습니다.
- 파일에는 **턴 숫자 크롭 그림과 읽은 값만** 들어갑니다. 게임 화면 전체나 도감 내용은
  안 들어갑니다.

## 단축키 (게임에 포커스가 있어도 동작합니다)

| 키 | 하는 일 |
|---|---|
| `Ctrl+Alt+O` | 숨기기 / 보이기 |
| `Ctrl+Alt+L` | 클릭 통과 (오버레이가 게임 조작을 방해하지 않게) |
| `Ctrl+Alt+R` | 턴 영역 다시 지정 |
| `Ctrl+Alt+←` `→` | 단계 앞뒤로 이동 |
| `Ctrl+Alt+Space` | 자동 켜기 / 끄기 |

손으로 단계를 옮겨도 **자동은 안 꺼집니다** — 옮긴 자리에서 계속 따라갑니다.

## 잘 안 읽힐 때

**[가르치기]** 를 누르면 지금 잡히는 화면과 함께 "실제 턴 숫자"를 넣는 칸이 나옵니다.
한 번 넣어 주면 그 화면에서 뽑은 숫자 모양이 대조표에 들어가, 그 뒤로는 훨씬 잘 읽습니다.
기본 대조표는 흔한 폰트로 만든 것이라 게임 폰트와 조금씩 다릅니다.

---

> **이 exe를 길드 밖으로 내보내지 말아 주세요.** exe 자체에 도감은 안 들어가지만,
> exe 옆에 `data/builds.json` 을 두고 통째로 넘기면 도감이 같이 나갑니다.
